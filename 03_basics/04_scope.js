function one() {
    const username = "vijay";

    function two() {
        const webiste = "youtube";
        console.log(username)
    }
    
    // console.log(webiste);
    two()
    
}

// one();

// ++++++++++++++++++++++++++++++++++++++++++++

function addone(num) {
    return 1 + num;
}
// console.log(addone(3));

const myNum = function(Number) {
    return 1 + Number;
}
console.log(myNum(2));